Max Goldstein - .vim directory

This folder contains the .vimrc configuration, my color scheme of choice (busybee.vim) and Pydiction plugins in the appropriate subdirectories.

There is a symbolic link to the .vimrc in my home folder. Create one using: rm .vimrc; ln -s ~/.vim/.vimrc .vimrc
